{
  "description": "Mon projet utilisant MongoDB avec PHP",
  "type": "project",
  "require": {
      "mongodb/mongodb": "^1.18",
      "monolog/monolog": "^3.0"
  },
  "autoload": {
      "psr-4": {
          "MonProjet\\": "src/"
      }
  }, 
  "scripts": {
    "compile": [
        "php bin/console doctrine:migrations:migrate"
    ]
  }
}
